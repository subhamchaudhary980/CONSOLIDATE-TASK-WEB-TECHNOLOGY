Unit 8: Embedding Multimedia Assets in Web-Based Projects

Here's what you need to know about consolidating task 2.
Consolidate task 2.
Right after setting up the folder, a fresh file named portfolio appeared. Starting from scratch, the basic HTML framework got built - <! DOCTYPE html>, <html>, <head>, <body> - just like needed for an HTML document.
Start with a formed name up top, followed by something brief that sets the scene below.
Inside the video tag sits a clip that plays while showing standard buttons for control. It links to a file named video.mp4 stored as an MP4.
A sound file played inside the page, pulled from audio.mp3, while buttons let users manage its playback.
A row of three pictures - img1.webp, img2.png, img3.jpg - takes shape through <img> tags. Flashing quickly in that space, a special area called the "skill badge" gets motion thanks to keyframe-driven styles in CSS.
